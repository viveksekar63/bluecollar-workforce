# Server-side URL used by the Next.js auth and API proxy routes.
# In Docker, use the backend service name, e.g. http://backend:3001/api/v1
BACKEND_API_URL=http://localhost:3001/api/v1

# Optional: kept for any future direct/public API integrations.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
